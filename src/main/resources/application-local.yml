spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bible_battle
    username: postgres
    password: postgres

  jpa:
    open-in-view: false

    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true